# .github/workflows/aport-verify.yml
name: APort Repository Guard

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, labeled, unlabeled, review_requested, review_request_removed]
  pull_request_review:
    types: [submitted, dismissed]

permissions:
  contents: read
  pull-requests: read
  id-token: write

jobs:
  aport-verify:
    name: aport-verify
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: APort Repository Guard
        # Pin this OIDC-enabled action to an audited commit.
        uses: aporthq/policy-verify-action@3c20b4ee48abc10995cb42c342b92235f58ca4b6
        with:
          mode: auto
